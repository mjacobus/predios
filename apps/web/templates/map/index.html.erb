<!DOCTYPE html>
<html lang="en">
  <head>
    <title>Prédios</title>
    <%= app_favicon %>
    <meta charset="UTF-8">
    <link href="<%= asset_url('building.ico') %>" rel="shortcut icon" type="image/x-icon">
    <script type="text/javascript">
      var MapData = {
        markers: {
          pin: "<%= asset_path('map/pin.png').gsub('&#x2F;', '/') %>"
        },
        buildings: <%= JSON.dump(buildings) %>
    };
    </script>
    <style type="text/css" media="screen">
      body { margin: 0 }
      #map {
        height: 100vh;
        width: 100%;
      }
    </style>
    <%= javascript 'global' %>
  </head>
  <body>
    <div id="map"></div>
    <script src="https://maps.googleapis.com/maps/api/js?key=<%= ENV.to_h.fetch('GOOGLE_MAPS_STATIC_API_KEY') %>&callback=initMap" async defer></script>
  </body>
</html>

