<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <title>Mapa</title>
    <script type="text/javascript">
      // <%= "<p></p>" %>
      // <%= raw asset_url('pin.png') %>
      // <%= URI.decode asset_url('pin.png') %>
      var MapData = {
        markers: {
          pin: "<%= _raw asset_url('map/pin_not.png').gsub('&#x2F;', '/') %>"
        },
        buildings: <%= JSON.dump(buildings) %>
    };
    </script>
    <style type="text/css" media="screen">
      body { margin: 0 }
      #map {
        height: 100vh;
        width: 100%;
      }
    </style>
    <%= javascript 'maps' %>
  </head>
  <body>
    <div id="map"></div>
    <script src="https://maps.googleapis.com/maps/api/js?key=<%= ENV.to_h.fetch('GOOGLE_MAPS_STATIC_API_KEY') %>&callback=initMap" async defer></script>
  </body>
</html>

